# Package or Module

    # use underscore "_" + module name

# Class

    # CamelCase

# Variable Type

    # CamelCase 
        # ex: T, AnyStr, Num

    # use varName + "_co" => for covariant
    # use varName + "_contra" => for contravariant

# Function, params, and var

    # snake_case
    # use mixedCase if there's style guide from related libraries

# Method and function instance

    # self for 1st argument in initiating method
    # cls for 1st argument in initiating methods
    
    # use blablabla + "_" if there's already reserved keyword

# Const

    # use UPPERCASE with underscore as the divider(?)
